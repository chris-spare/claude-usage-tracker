.build/
build/
.DS_Store
*.xcodeproj

# Defensive: runtime data normally lives under ~/Library, never the repo — but
# never commit logs, usage history, or the cached reading if they turn up here.
*.log
history.jsonl
history.json
usage-cache.json
